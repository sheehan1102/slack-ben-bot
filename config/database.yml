default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: slackbot_weather_development_db
  host: localhost

test:
  <<: *default
  database: slackbot_weather_test_db
